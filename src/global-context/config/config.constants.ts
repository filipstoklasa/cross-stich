import type { DefaultConfig } from "./config.types";

export const DEFAULT_CONFIG: DefaultConfig = {
  width: 500,
  height: 500,
  squareSize: 20,
  scale: 1,
  marker: { color: "#ff0000", symbol: "A" },
};
